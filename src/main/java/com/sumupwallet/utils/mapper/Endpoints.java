package com.sumupwallet.utils.mapper;

public class Endpoints {

    public static final String AUTH_CONTROLLER = "${api.prefix}/auth";
    public static final String TRANSACTIONS_CONTROLLER = "${api.prefix}/transactions";
    public static final String USERS_CONTROLLER = "${api.prefix}/users";
    public static final String WALLETS_CONTROLLER = "${api.prefix}/wallets";
}
